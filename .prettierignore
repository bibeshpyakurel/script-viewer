dist
package-lock.json

# Local, machine-specific editor and tooling state. Never part of the project.
.vscode
.idea

# Generated by `npm run export:json`. Its exact bytes are checked in CI, so
# Prettier must not reformat it out from under that check.
parsed

# Supplied fixture — must stay byte-for-byte as delivered. Never reformat.
src/fixtures/sample-script.xml
