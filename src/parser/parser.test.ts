import { describe, expect, it } from 'vitest';

// Placeholder so the test runner has something to execute until real
// parser tests land.
describe('parser', () => {
  it('runs the test suite', () => {
    expect(1 + 1).toBe(2);
  });
});
